# Étape 2.1 — correction Gamme ALPHA

- Restauration de l'IntersectionObserver de la V17.20.4 pour les blocs `.reveal`.
- La section Solutions n'est donc plus bloquée à `opacity: 0`.
- Les symboles SVG Gamme ALPHA présents dans `app.html` sont conservés.
- Header + Hero validés sont conservés.
